# Optional future AWS mode. The free local demo does not read this value.
VITE_APP_MODE=aws
# Public API Gateway origin used only by an explicitly enabled AWS build.
VITE_API_BASE_URL=https://example.execute-api.eu-west-1.amazonaws.com
